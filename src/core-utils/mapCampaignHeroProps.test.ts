import { describe, it } from 'vitest';

describe('mapCampaignHeroProps candidate acceptance cases', () => {
  it.todo('maps required Contentful fields to CampaignHeroProps');
  it.todo('maps responsive media metadata and normalizes asset URLs');
  it.todo('maps primary and optional secondary actions by style');
  it.todo('handles missing optional fields without inventing content');
});
